import PreviewUserInfo from '@/components/widgets/cards/PreviewUserInfo/PreviewUserInfo'

const ProfilePreview = () => {
  return (
    <div className='profile-preview' style={{ flexDirection: 'column' }}>
      <PreviewUserInfo />
    </div>
  )
}

export default ProfilePreview
